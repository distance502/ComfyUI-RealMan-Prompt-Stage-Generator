# 真男人提示词阶段生成器 使用说明书

本说明面向第一次安装的用户，也可作为参数和排错手册。节点显示名为 `真男人提示词阶段生成器`，位于 ComfyUI 的 `Qwen TE` 分类。

## 目录

1. [产品概览](#1-产品概览)
2. [安装与依赖](#2-安装与依赖)
3. [第一次生成](#3-第一次生成)
4. [界面与输出](#4-界面与输出)
5. [三种模型模式](#5-三种模型模式)
6. [提示词工作流](#6-提示词工作流)
7. [参考图片与角色设定图](#7-参考图片与角色设定图)
8. [视频提示词](#8-视频提示词)
9. [智能编排和一致性](#9-智能编排和一致性)
10. [参数参考](#10-参数参考)
11. [在线提示词搜索](#11-在线提示词搜索)
12. [NSFW 工作台](#12-nsfw-工作台)
13. [预设、历史与清空](#13-预设历史与清空)
14. [推荐配置](#14-推荐配置)
15. [故障排查](#15-故障排查)
16. [参数设置完整手册](#16-参数设置完整手册)
17. [分享与安全检查](#17-分享与安全检查)

## 1. 产品概览

这个节点不是普通的标签连接器。它会把标签、智能文本、随机主题、模板风格、参考图片、标签块和模型输出先归一到同一份创作主线，再生成自然语言图像提示词和连续视频分镜。

核心原则：

- 用户明确输入和锁定内容优先。
- 角色、场景、动作、道具、光线和镜头之间必须有可解释的关系。
- 随机只改动允许变化的部分，不破坏已锁定事实。
- 本地模型或 API 只在 Skill 底稿上补充和润色，不得另起题目。
- 模型失败或候选不合格时保留 Skill 成品，不返回空字符串。
- 图像、智能文本、视频和批量结果分别校验，避免相互串词。

适合的任务包括人物摄影、插画、动漫、概念设计、非人物产品或建筑、角色三视图、图片反推、批量灵感探索和多镜头视频剧情。

## 2. 安装与依赖

### 2.1 安装目录

将完整项目文件夹放到：

```text
ComfyUI/custom_nodes/ComfyUI-RealMan-Prompt-Stage-Generator
```

然后完整关闭并重启 ComfyUI。不要只刷新网页，因为 Python 路由和节点定义只有后端重启后才会重新加载。

仓库提供三个 Windows 脚本：

| 脚本 | 用途 |
| --- | --- |
| `内嵌安装到ComfyUI.bat` | 安装或更新节点；尽量保留用户标签和搜索配置 |
| `一键检查依赖.bat` | 检查当前 Python 环境，不安装任何包 |
| `自动补装依赖.bat` | 自动补装可选依赖 |

更新或手工复制时，只在 `custom_nodes` 中保留一份插件。常见的标准目录、旧目录和 GitHub ZIP 解压产生的 `-main` 目录同时存在，会导致前后端加载到不同版本。

### 2.2 可选依赖

| 依赖 | 什么时候需要 |
| --- | --- |
| `llama-cpp-python` | 节点直接加载本地 GGUF |
| `transformers`、`accelerate`、`safetensors` | 节点直接加载 Hugging Face 原始模型目录 |
| `opencv-python` | 图片质量分析 |
| `rapidocr-onnxruntime` | OCR 和文字伪影检查 |

`仅Skill` 不需要模型依赖。Ollama、LM Studio 和其他 HTTP 模型服务走 `API接口`，也不要求节点直接安装 `llama-cpp-python`。

### 2.3 模型文件位置

节点内置本地模型路线从以下目录读取：

```text
ComfyUI/models/LLM/
```

`.gguf` 文件走 `llama-cpp-python`；包含 `config.json`、tokenizer 和 `.safetensors/.bin/.pt/.pth` 权重的目录走 `transformers`。原始 Vision 模型使用目录内置的 `AutoProcessor`，不需要 GGUF `mmproj`；GGUF 视觉模型仍需要匹配的 `mmproj`，纯文本任务选择 `无`。模型与视觉投影必须来自兼容版本，不能随意混用。

## 3. 第一次生成

推荐先验证最短工作流：

1. 添加 `Qwen TE / 真男人提示词阶段生成器`。
2. 保持 `模型来源 = 仅Skill`。
3. 保持 `模板风格 = 自动`、`生成数量 = 1`、`提示词语言 = 纯中文`。
4. 点击底部 `标签`，至少选择一个主体、一个场景和一个画面风格。
5. 将 `首条正向提示词` 连接到下游正向文本编码，将 `推荐负面词` 连接到负向文本编码。
6. 运行 ComfyUI 队列，在节点终端的 `正向`、`负面` 和 `视频` 页签检查结果。

确认 Skill 路线正常后，再逐项打开智能文本、运行时随机、本地模型或 API。这样出现问题时容易判断是基础安装、模型还是参数造成的。

## 4. 界面与输出

### 4.1 底部按钮

| 按钮 | 作用 |
| --- | --- |
| `标签` | 按主体、服装、场景、构图、光影、画质等分组选择标签 |
| `随机跑` | 按当前随机策略生成一次变化 |
| `匹配启用` | 将智能文本解析并参与本次生成 |
| `模型` | 配置仅 Skill、本地模型或 API |
| `设定图` | 开启角色三视图或参考图设定图流程 |
| `随机` | 快速设置随机参数 |
| `示例` | 查看风格示例与推荐模板 |
| `预设` | 保存、载入和管理常用配置 |
| `NSFW` | 打开合法成年内容工作台 |
| `历史` | 查看、恢复、置顶或删除生成记录 |
| `回退` | 恢复上一步界面状态 |
| `槽位` | 管理标签槽位和自定义保留项 |
| `高级` | 展开模型、随机、语言、采样等参数 |
| `清空` | 清理当前临时输入和预览状态 |

每个标签分组最多可使用 20 个槽位。旧工作流的前 10 个槽位保持兼容，超出容量的预设内容会尽量转入自定义补充，而不是静默丢弃。

### 4.2 终端预览

终端页签用于直接检查本次执行：

- `正向`：最终图像正向提示词。
- `负面`：推荐负面词。
- `标签`：最终标签和模型/Skill 诊断。
- `编排`：标签块顺序与状态。
- `JSON`：结构化结果。
- `智能文本`：智能文本解析和统一后的正文。
- `视频`：多段自然语言视频分镜。

### 4.3 可选输入

- `qwen模型`：连接外部 Qwen、Gemma、Llama 等兼容模型对象。
- `参考图片`：连接 ComfyUI `IMAGE`，用于图片反推或角色设定图。

### 4.4 八个输出

| 序号 | 输出 | 说明 |
| --- | --- | --- |
| 1 | `结果全文` | 便于阅读的完整报告 |
| 2 | `首条正向提示词` | 第一条正向提示词，最适合直接连接下游 |
| 3 | `已选标签` | 最终标签、智能判断和模型链路诊断 |
| 4 | `JSON结果` | 适合脚本或其他节点读取的结构化数据 |
| 5 | `推荐负面词` | 根据人数、构图和任务生成的负面词 |
| 6 | `正向提示词合集` | 本轮所有正向结果 |
| 7 | `智能文本` | 智能文本输出 |
| 8 | `视频提示词` | 连续剧情的多段分镜 |

批量结果需要逐条处理时，可使用项目中的提示词合集拆分节点或下游自定义解析。

## 5. 三种模型模式

### 5.1 仅Skill

`仅Skill` 是默认和最稳定的模式：

- 完全离线即可生成。
- 图像、智能文本和视频都有可用输出。
- 不会调用本地模型或 API。
- 适合先验证工作流、低配置机器和对确定性要求高的任务。

Skill 不是简化占位符。它会完成标签收敛、剧情组织、关系检查、设定图结构、视频分镜和负面词生成。

### 5.2 本地模型

本地模型有两种接入方式：

1. 将兼容模型对象连接到 `qwen模型`。
2. 不连接外部对象，由节点从 `ComfyUI/models/LLM/` 直接加载 GGUF 或 Hugging Face 原始模型目录。

支持的模型家族包括 Qwen3-VL、Qwen3.5-VL、Qwen3.8-VL、Gemma4、Llama、Mistral、DeepSeek 和通用模型。`.gguf` 自动走 `llama-cpp-python`；选择包含 `config.json` 和权重的原始模型目录时自动走 `transformers`。模型文件名含 `qwen3.8` 或 `qwen38` 时也会识别为 Qwen3.8 家族。节点优先读取模型自带聊天模板；模板缺失、声明为空或处理器不兼容时，会根据模型家族选择保守兼容格式，并只进行有界重试。Qwen3.8 的专用视觉 handler 为可选能力；未安装对应版本时，节点不会因 `Invalid chat handler` 在加载阶段中断，而是继续使用模型自带模板或通用 Qwen 格式。

原始模型目录示例：

```text
ComfyUI/models/LLM/Qwen3.5-4B/
  config.json
  tokenizer.json 或 tokenizer.model
  model.safetensors（或多个分片）
```

原始 Vision 模型必须包含其对应的 processor/image processor；不要再为原始模型填写 GGUF `mmproj`。如果下拉框只显示权重文件，直接选择该文件也会自动使用同目录的 `config.json`。

因此，`Invalid chat handler: None` 不应再直接让整次任务失效。仍然失败时，图像和视频分别保留自己的 Skill 结果，并在 `已选标签` 和 `JSON结果` 中说明原因。

常用本地参数：

- `内置模型系列`：必须尽量与模型实际家族一致。
- `内置主模型`：选择 GGUF 文件，或选择带 `config.json` 的原始模型目录；`.safetensors/.bin/.pt/.pth` 权重文件也会自动解析到所在目录。
- `内置视觉投影mmproj`：GGUF 视觉任务选择匹配文件；原始 Vision 模型设为 `无`，纯文本也选 `无`。
- `内置上下文长度`：默认 8192；内存不足可降到 4096。
- `内置GPU层数`：`-1` 表示自动/尽可能卸载，显存不足时降低。
- `内置KV缓存K/V类型`：量化 KV 可降低显存占用。
- `内置批处理大小` / `内置微批处理大小`：分别对应 `n_batch` / `n_ubatch`，显存不足时降低。
- `内置线程数` / `内置批处理线程数`：`0` 表示让 llama.cpp 自动决定。
- `内置Flash注意力`：默认自动；运行时明确支持时可开启。
- `内置KQV卸载`、`内置内存映射`、`内置锁定内存`：控制 GPU KQV、mmap 和 mlock。
- `内置RoPE频率基值` / `内置RoPE频率缩放`：`0` 使用模型元数据，普通使用不要修改。
- `内置模型参数JSON`：GGUF 可填写其他 Llama 构造参数，例如 `{"n_seq_max": 2, "tensor_split": [0.6, 0.4]}`；原始模型可填写 `transformers` 参数，例如 `{"torch_dtype": "bfloat16", "device_map": "auto", "attn_implementation": "sdpa"}`。不支持或受保护的键会忽略。
- `内置启用思考`：普通提示词建议关闭，减少分析文本干扰。
- 原始模型额外依赖：运行 `自动补装依赖.bat`，或在 ComfyUI 的 Python 中安装 `transformers accelerate safetensors`。

### 5.3 API接口

API 路线支持 OpenAI-compatible 服务、OpenAI、OpenRouter、DeepSeek、DashScope、Kimi、SiliconFlow、火山方舟、智谱等预设，也支持 Claude 和 Gemini 原生适配。Ollama、LM Studio 和其他本机服务通常使用兼容地址。

配置步骤：

1. 选择 `API服务商`。
2. 使用预设地址，或填写规范的 HTTPS/Base URL。
3. 填写模型名。
4. 推荐将密钥放在环境变量，并把 `API密钥` 写为 `env:变量名`。
5. 首次测试使用 `生成数量 = 1`，确认后再增加批量。

推荐示例：

```text
env:QWEN_TE_API_KEY
```

通义千问原生调用建议使用：

```text
API服务商：通义千问DashScope
API地址：https://dashscope.aliyuncs.com/api/v1
API密钥：env:DASHSCOPE_API_KEY
API模型：qwen3.7-max
```

模型名不受节点白名单限制，可填写账号所在地域实际开通的通义文本或视觉对话模型。文本模型自动调用 DashScope Generation，Qwen VL、QVQ、Omni 或包含图片的请求自动调用 MultiModalConversation；Qwen3、QwQ、QVQ 会自动发送 `enable_thinking=true`，但提示词只读取最终 `content`，不会混入 `reasoning_content`。直接填写完整原生端点也受支持。

旧工作流可继续填写 `https://dashscope.aliyuncs.com/compatible-mode/v1`，节点会自动补全 `/chat/completions` 并按 OpenAI-compatible 协议发送。只有 `Generation` / `MultiModalConversation` 类型的文本和视觉对话模型属于此节点的调用范围；通义万相图像生成、视频生成、语音合成等产品使用不同任务 API，不应把它们的模型名填入提示词对话接口。
本地 GGUF、外接本地服务和 API 统一经过 `model-call` Skill。它负责规范消息、选择可用文本通道、抽取最终正文、清洗思考块并记录调用通道；瞬时网络错误、参数兼容问题和候选校验仍按原有重试与 Skill 回退链路处理。图片反推的图文消息也通过同一入口，保留原始图片内容，不会被改写成纯文本。

自定义地址不得包含账号密码、查询参数或 URL 片段。环境变量密钥只允许发送到受信来源；确需把密钥发送到自定义来源时，应通过 `QWEN_TE_CUSTOM_API_SECRET_ORIGINS` 明确授权。

### 5.4 模型和 Skill 如何协作

所有模型路线都按以下顺序执行：

```text
用户输入与锁定事实
        ↓
Skill 建立关系图和可用底稿
        ↓
本地模型或 API 增量润色
        ↓
清洗分析文本、格式和占位符
        ↓
锚点、一致性与自然语言校验
        ↓
采用、定向修复、融合或 Skill 回退
```

“调用成功”不等于“必须采用”。模型改掉主体、遗漏场景、返回标签串、破坏三视图、生成无连续性的分镜时，保留 Skill 结果是正常保护行为。

## 6. 提示词工作流

### 6.1 标签生成

标签用于告诉系统“画面里必须有什么”，而不是要求最终输出也保持标签格式。建议每个关键分组选 1-3 个互补项：

- 主体：身份和稳定外观。
- 画面风格：只保留一个主媒介。
- 服装造型：说明轮廓和材质。
- 动作姿态：选择当前可见动作。
- 场景背景：明确一个主地点。
- 道具世界观：只放能与动作或场景发生关系的道具。
- 光影氛围：主光、色调或空气状态。
- 构图视角：景别、朝向、机位。
- 技术画质：1-2 个互补成像目标。

不要同时选择多个相互竞争的主场景、动作、媒介和主光。智能系统会尽量收敛自动来源，但不会擅自删除用户明确选择的冲突项。

### 6.2 智能文本

在智能文本输入框写自然语言，然后点击 `匹配启用`。例如：

```text
单人女冒险者背对镜头站在地下城石桥上，风很弱，火炬是唯一主光，保持黑白暗黑漫画风格，不要现代设备。
```

系统会识别正向事实、明确排除、主体人数、朝向、姿态、场景、道具、光线和媒介，并与节点标签合并。

`智能文本风格优先`：

- `自动判断`：根据文本明确程度自动选择。
- `节点优先`：文本补细节，不替换当前模板主线。
- `文本优先`：明确文本可以接管低优先级模板或随机结果。

### 6.3 运行时随机

| 模式 | 行为 |
| --- | --- |
| `自动判断` | 根据当前任务和锁定内容选择合理变化范围 |
| `全随机` | 大幅改变未锁定的主体、场景、服装和镜头 |
| `保留已选核心标签` | 保留指定数量的核心标签，再补随机变化 |
| `重写主体与场景` | 重点替换主体身份、地点及其从属内容 |

`核心标签锁定数量` 只限制保留核心模式；白名单、设定图事实和 NSFW 保护项不受该数截断。想要明显变化时设为 `0-3`，系列角色可设 `4-10`。

随机强度控制变化幅度。`随机主题池` 提供题材和地点，`模板风格` 提供媒介和成片质感。两者同时固定时，模板风格优先决定媒介，主题池不能把其他画风带进来。

`随机排除标签` 可写不希望随机出现的内容；它不会删除白名单和用户显式事实。`seed = 0` 适合每次变化，正整数用于复现。历史避重也参与结果差异，因此严格复现还需要相同的工作流和历史状态。

### 6.4 标签块编排

打开终端中的 `编排`，可将标签和自然语言整理为可拖动的块。适合：

- 固定“主体 → 服装 → 动作 → 场景 → 光线 → 镜头”的阅读顺序。
- 锁定不能被随机替换的块。
- 在标签块之间加入自然语言要求。
- 将现有策划文本拆成可管理段落。

开启 `标签块编排启用` 后，`标签块编排JSON` 由界面维护。不要把普通提示词直接粘贴进 JSON 输入，也不要手工截断结构。

### 6.5 风格隔离

- `平衡收敛`：默认。清理明显冲突，保留合理混合。
- `严格风格隔离`：系列图和明确媒介任务使用，阻止其他强风格进入。
- `允许风格漂移`：实验性混合，接受更大风格变化。

减少串词的关键不是堆更多负面词，而是只保留一条主媒介、一个主场景和一套可解释的道具关系。

## 7. 参考图片与角色设定图

### 7.1 图片反推

连接 `参考图片` 后开启 `图片反推生成`：

- `仅反推描述`：提取画面可见事实，作为智能文本进入普通生成。
- `角色设定图`：提取角色身份、体型、发型、服装、材质和配色，再生成三视图。

`图片反推最大边长` 默认 960。提高会增加视觉 token、内存和耗时；大多数参考图使用 768-1024 即可。

图片反推只描述可见内容。无法确定的身份、品牌、材质或遮挡区域不应凭空补写；OCR 检出的水印、字幕和界面文字不会成为角色服饰。

### 7.2 三视图固定合同

默认角色设定图必须满足：

1. 画幅分成 `1:1:1` 三个等宽区域。
2. 左侧为正面全身，面向相机。
3. 中间为精确 `90°` 标准侧面全身。
4. 右侧为背面全身。
5. 三个视图是同一个角色，体型、面部、发型、服装、配色和材质一致。
6. 三个人物等高，共用头顶线、脚底基线和镜头高度。
7. 使用正交相机，避免广角变形。
8. 使用连续、简洁、中性的背景，不加入独立场景和无关道具。

参考图反推生成三视图时遵守同一结构。推荐横向宽画幅、数量 1、关闭运行时随机，并使用无遮挡的单人全身参考图。

只有在额外要求明确提出时，才增加表情、头像或材质细节带。辅助内容不能破坏三幅主视图的等宽关系。

### 7.3 普通单图与三视图不要混用

设定图关闭时，普通单图不会自动生成多视角。普通人物图不要同时写“正面、侧面、背面”“四联画”“角色表”等要求；否则下游模型容易生成重复人物、分屏和多张脸。

## 8. 视频提示词

### 8.1 输出结构

视频提示词是独立故事，不是图像提示词的机械扩写。默认至少三段、通常五段分镜：

1. 建立场景与人物状态。
2. 事件或变化触发。
3. 主体采取行动。
4. 阻力、风险或情绪升级。
5. 结果、余韵或开放定格。

每段应包含：

- 从 1 开始且连续、唯一、按顺序排列的分镜序号；
- 可识别的阶段标题按建立、触发、行动、升级、收束排列；自定义标题可以保留，但不得把明确的触发阶段标到收束镜头；
- 景别、机位或拍摄关系；
- 摄影机运动或固定方式；
- 主体当前动作及动作结果；
- 环境、光线、材质、声音中的至少一种反馈；
- 与上一镜头的空间、动作或因果连续性。

字数不限，也不强制写固定时长。只有用户明确要求时才加入秒数、帧率或平台格式。

完整模型故事板出现重复编号、倒序、跳号、阶段标题倒序或编号段与无编号段混杂时，节点会要求模型定向修复，仍不合格则保留 Skill 结果。局部模型增量可以只写一个或两个明确编号的分镜，例如 `分镜四`；节点会按编号并入对应镜头，不会按返回位置误写到其他段落。若同时写了可识别的阶段标题，编号和阶段必须一致，例如 `分镜四（触发）` 会被判断为阶段错位；无法识别的自定义标题不会被强行归类。未写编号的一两段短增量会从中英文自然语言判断它属于建立、触发、行动、升级还是收束，并写入对应镜头；“不是结尾，而是冲突升级”等否定表达会先按分句消歧。多段增量若被判断为时间倒序，不会自动调换原文，而是把具体阶段顺序交给模型定向重试，仍失败才保留 Skill 底稿。成功融合后，推理纠偏说明会显示 `分镜2（触发）` 一类实际落镜诊断。

### 8.2 连续性规则

- 首镜必须建立主体、服装、主场景和关键道具；后续每镜可用全称、代词、同一空间参照、材质反馈或可见状态承接，不要求机械重复标签。
- 同一角色的服装、道具和伤痕不能无原因改变；撕裂、沾湿、脱落、穿回或明确换装等有动作因果的变化允许通过，且因果动作必须直接修饰服装，不会借用同镜头中门、墙等环境物件的动作词放行；`凭空变成白色礼服` 一类无解释换装会触发定向修复。
- 道具必须有来源和去向；拾取、放下、交接、丢失、破损和熄灭等明确变化允许通过。新出现的物件会按实际名称与已选道具比对，已有信封或箱子被打开后露出夹层线索可以通过，未选中的枪械、钥匙或其他物件凭空出现，以及关键道具凭空消失，会触发定向修复。
- 新主体按实际身份短语检测；同一分镜继续写原角色，不能掩盖后文引入的`另一名陌生男子`或 `another guard`。`不再引入无关人物`、`without introducing another guard` 等否定约束不会被当成真实新增人物。
- 原主体不能无解释变成男孩、陌生人、机器人、石像或其他身份；这类身份漂移会优先于“新增主体”检查并触发定向修复。只有动作标签或剧情锚点明确包含变身、变形、形态转换、`morph` 或 `shapeshift` 时，身份变化才会作为有意剧情通过。动作锚点指定目标后，候选必须使用同一目标或中英文同义身份，例如石像、雕像和 `statue` 可以互认，但“变身为石像”不能放行变成男孩；未指定目标的通用变身动作保持开放。该许可只作用于重叠的身份变化文本，不会连带放行同镜头中无关的凭空换装。
- 摄影机可以切换，但人物方位、视线和运动方向要可衔接。
- 同一地点内部沿可见路径移动、切到人物手部或脸部特写可以继续故事；`特写`、`内部`、`连续承接上一镜` 等弱镜头或叙述词不能掩盖新的地点名，跨区域必须说明同一空间、相邻关系或沿可见路径移动，无承接地切换到另一地点或引入陌生主体会被拒绝。
- 光线和天气变化必须有时间、空间或事件原因。
- 多镜头可以有景别变化，不等于在单张图里生成漫画分格。
- 不属于当前世界的物件不能只为“丰富画面”被模型加入。

### 8.3 视频模型后置状态

可在 `已选标签` 或 `JSON结果` 查看：

- `未调用（仅Skill）`：预期行为，视频 Skill 已生成结果。
- `已采用模型润色`：模型候选通过结构、剧情和锚点校验。
- `模型调用成功，保留 Skill 结果`：模型响应存在，但变化不可靠或没有增益。
- `模型调用失败或候选不合格，保留 Skill 结果`：请求失败、内容含分析、少于三段、分镜编号不连续、缺少锚点，或逐镜主体/场景/道具连续性被破坏。

视频调用失败不会清空第 8 个输出，也不会影响前 7 个输出。

## 9. 智能编排和一致性

### 9.1 优先级

从高到低：

1. 用户明确文本、手选标签和白名单。
2. 角色设定图、图片反推的可见事实和锁定编排块。
3. NSFW 工作台保护锚点。
4. 运行时随机、主题池和模板档案。
5. 模型新增细节。

只有低优先级且来源可证明的自动内容会被安全清理。两个手动事实互相冲突时，系统会报告并采用保护性 Skill 策略，而不是猜测用户想保留哪一个。

### 9.2 关系域

当前智能引擎统一检查以下关系：

| 域 | 状态示例 | 防止的问题 |
| --- | --- | --- |
| 主体数量 | 单人、双人、群像、无人 | 路人、克隆主体、人物污染非人物任务 |
| 朝向与姿态 | 正面/侧面/背面，站/坐/跪/躺/蹲 | 模型改掉视角或出现物理不成立的姿态 |
| 动作支撑 | 坐在台阶、倚靠墙面、手持火炬 | 动作缺少接触物或道具悬空 |
| 场景世界 | 森林、地下城、城市、室内/户外 | 现代设备混入地下城、地点无故切换 |
| 环境连续 | 季节、昼夜、降水、风、温度、地表 | 雨中干湿矛盾、室内暴雪、无风烟尘横飞 |
| 光线 | 自然/人工主光、方向、硬软、色温 | 火炬场景出现无来源棚灯或相反阴影 |
| 色彩成像 | 黑白/彩色、曝光、对比、饱和、颗粒、锐度 | 模型把黑白漫画改成高饱和照片 |
| 镜头 | 景别、机位、焦段、投影、景深、焦点平面、镜头稳定性 | 全身变特写、正交变鱼眼、焦点主体错误或手持/固定状态被改写 |
| 空气与背景 | 通透、雾、烟尘、背景复杂度 | 凭空加入浓雾或在设定图塞满场景 |
| 运动表现 | 凝固、运动模糊、慢门拖影 | 单一轮廓和连续位移带互相混用 |

### 9.3 静态约束与变化剧情

智能系统只在最终状态唯一时锁定。以下内容保持开放：

- “先坐后站”“从雨夜走到晴朗清晨”等明确时序变化。
- 冷暖对比、硬主光配柔补光等混合设计。
- 主体清晰而背景拖影、追焦等分区运动表现。
- 2D/3D 融合、局部彩色、前后景不同清晰度。
- 三视图同时包含正面、侧面和背面。

普通文本中同时出现多个状态但没有变化词时，仍可能被判断为冲突。应写明“先后”“转为”“局部”“主光/补光”或具体空间分区。

### 9.4 定向修复

模型候选只违反某个关系轴时，二次请求只处理该轴。例如：

- 风势修复只调整发丝、衣摆、植被、雨雪和既有空气介质的受力。
- 焦点修复只调整主体、前景或背景的清晰层级。
- 光质修复只调整阴影边缘、高光过渡和表面响应。
- 运动修复只调整轮廓是否单一、位移是否连续和事件相位。
- 镜头稳定性修复只调整帧间背景锚点、地平线和取景中心的规则或不规则位移。
- 焦段透视修复只调整近远尺度差、前中后景层叠和视轴空间压缩或延展。
- 曝光调性修复只调整全局亮度分布、暗部面积和黑位关系，不改变光质或整体对比度。
- 整体对比度修复只调整全局明度跨度、中间调宽度和直方图分布，不改变曝光调性、光质、局部对比或 HDR 表现。
- 整体饱和度修复只调整全局综合色度分布、中性灰占比和鲜艳色覆盖关系，不改变冷暖色相、黑白媒介、局部彩色或肤色控制。
- 成像颗粒修复只调整平坦区域的噪点分布、全画面颗粒尺度和轮廓连续性，不改变材质表面纹理、雨雪尘埃或扫描印刷纹理。
- 整体成像锐度修复只调整轮廓过渡宽度、细线分离度和微纹理边缘响应，不改变景深虚化、运动模糊、雾气或局部皮肤处理。
- 整体细节密度修复只调整单位区域结构数量、纹理层级和简化区域占比，不改变分辨率、锐度、颗粒、局部精细化或远近 LOD。
- 画面媒介修复只调整轮廓生成方式、表面响应和采样特征，不改变混合媒介、2.5D、局部内嵌画面、投影几何或明确媒介转场。
- 投影几何修复只调整平行线汇聚、同尺寸对象的深度尺度变化、轴测轴线尺度和鱼眼径向弯曲关系，不改变焦段、机位、辅助视图、混合投影或投影转场。
- 透视消失点结构修复只调整一点、两点、三点透视的水平消失点数量和竖直线汇聚关系，不改变投影类型、焦段、机位、辅助视图、混合透视或连续转场。
- 画幅滚转修复只调整重力竖直方向、地平基准和全局滚转角，不改变高低机位、透视消失点、焦段、局部倾斜物体、辅助视图或连续滚转转场。
- 主体构图落点修复只调整主体视觉重心、左右质量平衡和视线/动作负空间，不改变景别、机位、画幅滚转、局部不对称物体、辅助视图或连续重心转场。
- 横向屏幕方向修复只调整主体指向、前导空间和运动轨迹方向，不改变主体朝向、景别、机位、画幅滚转、构图落点或剧情顺序；明确越轴、转身或反向动作时保持转场。
- 空间轴线连续性修复只调整180度动作轴线、摄影机半空间与角色左右位置，不改变主体身份、视线、横向运动、景别、机位、构图落点或剧情顺序；固定不越轴时不会自行换边，只有明确的连续越轴路径才允许交换画面左右位置。
- 前景遮挡关系修复只调整主体无遮挡与受控前景框景，不改变服装覆盖、手持道具接触、景深、雾气、降水能见度、景别或机位；框景只能使用当前场景已有元素，不能自行增加门框、家具、植被或肩后人物遮住脸、手和动作支点。
- 主体支撑状态修复只调整稳定承重接触与整体离地悬浮，不改变站坐跪躺姿态、动作内容、运动模糊、景别或机位；悬浮时必须保留离地间隙、统一受力与不接触的阴影线索。运行时随机人物图在没有明确离地依据时默认使用可信承重，并按姿态建立唯一承重点：站姿和蹲姿使用脚与当前地表，跪姿使用膝部与当前地表；坐姿和躺姿只有在自然语言明确接触某个表面，或动作、用户补充、道具层只提供一个可用承托物时才引用它，否则使用当前地表。场景背景里的家具只登记为后续可用物件，不会自动建立身体接触；同一优先层存在多个候选承托物时也不会按标签顺序猜测。用户明确写出的主体或承重点、接触动作与具体表面关系优先于家具的普通场景提及，适用于站、坐、跪、躺、蹲全部姿态；远景沙发、房间另一侧的床、人物身旁的椅子不会被误当成当前承托面。该关系会写入 Skill、API、本地模型及标签块提示词，不会为了承重凭空增加家具、台阶或平台；同时服从当前景别，全身或远景显示实际接触点，近景或半身只用画内骨盆、脊柱和肩线维持承重连续性，不会为展示画外脚部、膝部或承托面而扩大取景，未指定景别时也不会反向强制全身构图。模型候选还会联合检查身体承重点、接触动作和具体表面，背景里单独出现物件或人物站在物件旁不会误报；单张图片只保留当前承托瞬间，带分镜编号和时序词也不能换面；视频只有在编号分镜中明确写出起身、坐下、跪下、躺下、迈上或走上等实际承重变化，并交代旧接触解除、重心移动和新接触建立时，才能使用场景里本来就有的另一承托面，单独的“然后、随后、接着、转而”不构成有效转场。显式跳跃、飞行、水下悬停、失重主题不会被默认值覆盖，空气水汽、灰尘、漂浮花瓣、浮空建筑和漂浮道具也不会触发人物悬浮。
- 多人承重覆盖会把支撑合同分别应用到双人或群像中的每位可见人物，每个人都保留独立承重点、重心投影与接触反馈，不能只让其中一人着地，也不能用另一人的身体代替地面或承托物。多人具有不同姿态时会逐人建立计划：站姿和蹲姿使用双脚，坐姿使用髋部，跪姿使用膝部，躺姿使用躯干；每种姿态只绑定自己已经确定的承托面，例如坐在沙发与躺在床会形成两条不可互换的关系。两人姿态相同但表面不同时，会进一步按人物指代绑定承托关系：支持“一人/另一人、第一人/第二人”及 `one person / the other person`，可区分两名坐姿人物各自使用沙发和椅子，也可区分两名站姿人物各自使用石台和地面；模型交换人物与承托面会触发定向修复。明确人数的三人、四人群像继续支持第三位、第四位及英文 `third/fourth person`，但只有从第一位到最后一位都明确写出姿态与表面时才启用逐人绑定；人数不明或漏写任一成员时使用共享关系，不臆测缺失成员。互不重复的职业身份也可替代序号作为稳定成员键，当前覆盖战士、法师、骑士、游侠、弓箭手、盗贼、刺客、祭司、侦探、冒险者、旅人、摄影师和学者及其英文表达；职业数量必须与明确人数一致，同职业双人不会仅凭男女前缀强制拆分。多人明确共用同一表面时仍保留共享关系，不会机械拆分。近景或半身允许实际接触点留在画外，不会为展示多人脚部或家具而扩大景别。图片保持同一瞬间的逐人承重，视频逐镜保持每人的支撑状态，只有编号分镜中明确作用于对应人物的承重变化才能改变其承托面。明确的一人飞行、一人站立等混合状态只约束着地成员，不会被统一改成全部着地。
- 视线目标修复只调整镜头光轴、瞳孔方向与场景目标关系，不改变面部朝向、主体姿态、屏幕方向、景别或机位；明确转头、回望、双人对视或视线转移时保持转场。

定向修复不会重写主体身份、场景、动作内容、镜头顺序和其他已正确事实。修复后仍不合格才回退 Skill。

### 9.5 诊断信息

`已选标签` 中重点查看：

- `智能编排`：任务识别、世界族、模型策略和风险。
- `智能定向修复`：本轮修复次数与类型。
- `模型实际来源`：仅 Skill、外接模型、内置 GGUF 或 API。
- `模型调用状态`：调用、采纳和回退情况。
- `模型与Skill链路`：具体失败或校验原因。
- `推理纠偏`：低优先级内容如何被收敛。

`JSON结果` 还提供关系图、来源、策略和修复字段，适合自动诊断。

## 10. 参数参考

### 10.1 基础生成

| 参数 | 默认 | 建议 |
| --- | --- | --- |
| `模板风格` | 自动 | 系列图选择具体模板，探索保持自动 |
| `主体类型` | 自动 | 人物或非人物任务不明确时手动指定 |
| `案例输出结构` | 自动 | 长段图像提示词用案例长段版 |
| `生成数量` | 3 | 日常 1-4，批量探索 4-8，最大 20 |
| `提示词语言` | 纯中文 | 按下游模型习惯选择 |
| `详细度` | 标准 | 只改变信息密度，不限制字数 |
| `输出模式` | 完整结果 | 下游只要正文时用仅提示词优先 |
| `标签反推模式` | 自动平衡 | 普通任务保持默认 |
| `风格隔离策略` | 平衡收敛 | 系列图使用严格风格隔离 |
| `优先柔和肤质` | 关闭 | 人像需要时开启 |
| `抑制文字伪影` | 关闭 | 海报、招牌或易出乱码时开启 |

### 10.2 随机与锁定

| 参数 | 默认 | 建议 |
| --- | --- | --- |
| `运行时随机标签` | 关闭 | 批量灵感时开启 |
| `运行时随机模式` | 自动判断 | 大改用全随机或重写主体与场景 |
| `核心标签锁定数量` | 10 | 大变化 0-3，稳定角色 4-10 |
| `运行时随机强度` | 中 | 先调强度，再调模型温度 |
| `随机主题池` | 自动 | 明确题材时选具体池 |
| `锁定标签白名单` | 空 | 只放真正不可变的锚点 |
| `随机排除标签` | 空 | 写明确禁止从随机池出现的内容 |
| `seed` | 0 | 变化用 0，复现用正整数 |

白名单过多、核心锁定过高、模板和主题池都固定时，随机变化会很小。这不是模型锁死，应先放松锁定，再提高随机强度。

### 10.3 智能文本、编排和反推

| 参数 | 作用 |
| --- | --- |
| `智能文本匹配` | 是否解析智能文本并加入生成 |
| `智能文本输入` | 自然语言事实、排除和剧情要求 |
| `智能文本风格优先` | 自动判断、节点优先或文本优先 |
| `标签块编排启用` | 是否按编排块参与统一链路 |
| `标签块编排JSON` | 由编排界面维护的结构化数据 |
| `图片反推生成` | 连接参考图片后启动视觉反推 |
| `图片反推模式` | 角色设定图或仅反推描述 |
| `图片反推最大边长` | 视觉输入缩放上限，默认 960 |

### 10.4 模型采样

| 参数 | 默认 | 推荐范围 | 注意 |
| --- | --- | --- | --- |
| `最大生成token` | 1800 | 长文 1800-2600 | 太低可能截断；Skill 不受此限制 |
| `温度` | 0.62 | 稳定 0.45-0.65，多样 0.72-0.9 | 过高容易偏题 |
| `top_p` | 0.9 | 0.85-0.95 | 不要和温度同时大幅拉高 |
| `top_k` | 40 | 20-80 | 某些 API 不支持时会省略 |
| `重复惩罚` | 1.08 | 1.05-1.15 | 过高会破坏专有名词 |
| `频率惩罚` | 0 | 复读时 0.1-0.3 | 过高会让句子生硬 |
| `存在惩罚` | 0 | 扩展时 0.05-0.2 | 过高会不断换主题 |
| `输出think块` | 关闭 | 日常关闭 | 只用于诊断，可能污染阅读 |

优先只调整温度。仍然复读时，小幅增加频率惩罚；不要一次把所有采样参数拉满。

### 10.5 系统提示词覆盖

默认系统提示词已经包含自然语言、剧情、语言、一致性和用户事实合同。除非明确知道模型需要什么格式，否则保持默认。覆盖内容如果要求标签串、极短输出或不同结构，会触发校验回退。

## 11. 在线提示词搜索

节点界面不再提供网页浏览器或独立浏览器入口。在线搜索仅保留后端接口供外部工作流调用；结果不会自动写入提示词，外部来源不可用时会回退本地标签库联想。

`online_search_config.json` 关键项：

| 配置 | 说明 |
| --- | --- |
| `searxng_base_url` | 自己的 HTTPS SearXNG 地址 |
| `searxng_timeout_seconds` | 搜索超时，通常 8-20 秒 |
| `public_source_fallback_enabled` | `false` 时禁用 Civitai/Lexica 公共回退 |

也可在启动 ComfyUI 前设置 `QWEN_TE_SEARXNG_URL` 临时覆盖地址。实例必须使用 HTTPS，工作流和前端不能指定任意搜索服务器。

## 12. NSFW 工作台

NSFW 工作台用于管理合法成年内容的结构化标签、镜头和负面方案。未启用时不会参与普通生成。

基本流程：

1. 点击 `NSFW`。
2. 选择成年身份、关系、服装、动作、场景、镜头、光线和质量项。
3. 点击启用工作台。
4. 点击 `写回当前节点`，需要时选择同步负面词。
5. 回到主节点运行。

优先级为手动字段与选择器 > 预设填充 > 质量层级。预设只填空字段，不覆盖手选内容。自定义字段库只服务 NSFW 工作台，不会自动污染普通标签库。

器官和显式行为必须主动选择，不会被“全部随机”自动抽取。系统还会保持成年主体、人数、单帧身份和人物不复制规则。请勿用于未成年人、年龄不明确或违法内容。

`动作关系`、`动作` 和 `成人动作` 都提供完整动作合同，覆盖正面、后方、女上位、侧卧阴道插入，后方与侧卧肛门插入，异性及双成年同性角色的单向/相互口部刺激、手部刺激，成年男性、成年女性单人自慰，以及单人或伴侣辅助的成人道具刺激与插入。完整合同会把成年主体、身体部位、动作对象、道具持有者、接触点和承重姿态写成一条自然语言关系，不再把器官词与行为词拆成互不关联的标签。

兼容旧工作流时，系统会按 `露骨词` 中最先出现且条件完整的行为收敛为一条主合同。异性动作要求明确选择 `成年情侣`、`成熟夫妇`、`男女配对` 或同时指定成年男性与成年女性；同性动作只接受 `双成年女性` 或 `双成年男性`，不会把年龄不明确的 `双女性` 自动升级。单人自慰和成人道具动作只在成年角色与目标部位匹配时合成，伴侣辅助道具还要求明确的成年双人关系。关系不明确、器官目标不足的配置不会被猜测成其他动作。
结果词可选择 `女性刺激潮吹结果`、`男性刺激射精结果`、`受控体液结果`，也兼容旧的 `潮吹`、`射精`、`体液`。它们不会单独创建动作：只有基础动作合同成立且目标部位匹配时，才会作为同一条自然语言合同的“动作结果阶段”附加，并固定结果来源、落点、人物数量、接触点和镜头轴线；否则不会凭空推断主体关系。

## 13. 预设、历史与清空

### 13.1 预设

预设用于保存风格、主体、随机策略、智能文本、标签块、图片反推、本地模型、API 服务商/地址/模型、超时和采样参数。

预设不会保存：

- API 密钥；
- API 额外认证头；
- 随机和连续避重缓存；
- 运行时预览令牌。

完整 ComfyUI 工作流仍可能保存输入控件里的明文密钥，公开分享前必须检查。

预设最多保留 30 项，单项和总容量有上限，避免浏览器本地存储无限增长。

### 13.2 历史

历史最多保留 50 项，可查看提示词、恢复状态、置顶和删除。每次执行会尽量保存最终正向提示词、必要参数和批量执行快照。

恢复历史后，先检查模型/API 环境和参考图片是否仍存在；历史不能恢复已经删除的外部模型文件或环境变量。

### 13.3 清空

`清空` 会清理当前临时输入、终端预览、编排临时状态、额外要求和匹配结果。它不会删除已保存预设、已安装模型、历史记录或 NSFW 自定义字段库；这些内容需要在各自面板中明确删除。

## 14. 推荐配置

### 14.1 稳定离线

```text
仅Skill + 生成数量 1-3 + 标准 + 随机关闭 + 平衡收敛
```

### 14.2 批量找灵感

```text
运行时随机开启 + 自动判断/全随机 + 强度中或强
核心锁定 0-3 + seed 0 + 自动主题池
```

### 14.3 固定系列角色

```text
具体模板 + 具体主题池 + 严格风格隔离
白名单锁定身份/服装 + 随机弱 + 固定正数 seed
```

### 14.4 本地低显存

```text
7B/8B Q4 GGUF + 上下文 4096-8192 + 数量 1
token 1280-1800 + 图片边长 640-768 + 量化 KV
```

### 14.5 API 长剧情

```text
API接口 + 生成数量 1-3 + 详细
token 1800-2600 + 温度 0.62-0.78 + top_p 0.9-0.95
```

### 14.6 角色三视图

```text
设定图启用 + 生成数量 1 + 横向宽画幅
运行时随机关闭 + 简洁背景 + 清晰单人全身参考图（可选）
```

## 15. 故障排查

### 15.1 找不到节点或完整界面不显示

1. 确认目录中有 `__init__.py`、`stage_prompt_generator.py` 和 `web`。
2. `custom_nodes` 中只保留一份本插件，移走旧目录和 `-main` 重复目录。
3. 完整关闭所有 ComfyUI 后端窗口后重启。
4. 强制刷新 ComfyUI 页面（`Ctrl+F5`）。
5. 查看启动日志是否有 Python 导入错误。

完整主面板未接管时可能显示 `TE MINI CONSOLE` 兼容工具栏。可在地址后加入 `?qwen_te_mini=1` 强制备用模式。备用模式仍应保留标签、随机、入队、槽位、高级、NSFW 和清空等基本操作。

如果兼容工具栏下方仍展开 `主体标签11-20` 等大量原生槽位，说明前端脚本缓存未更新或存在重复插件。清理重复目录、完整重启后再强制刷新页面。

### 15.2 ComfyUI 8188 无法访问

先确认后端是否监听：

```powershell
netstat -ano | findstr 8188
```

没有结果说明 ComfyUI 未成功启动，应检查启动窗口中的第一条异常。

### 15.3 本地模型无法加载

检查：

- GGUF 是否仍在 `ComfyUI/models/LLM/`。
- `llama-cpp-python` 是否安装在 ComfyUI 实际使用的 Python 中。
- 模型家族是否选择正确。
- 视觉任务的 mmproj 是否匹配。
- 上下文长度是否至少 1024。
- 是否因 GPU 层数、上下文或图片过大造成内存不足。

先切回 `仅Skill` 验证基础链路，再把数量设为 1、上下文降到 4096、图片边长降到 640-768 重试。

### 15.4 出现 Invalid chat handler

当前版本会优先读取模型自带模板，并对 Qwen、Gemma、Llama、Mistral、DeepSeek 和通用 GGUF选择兼容回退。如果仍出现该错误：

1. 确认模型系列与文件实际家族一致。
2. 更新 `llama-cpp-python` 到与模型格式兼容的版本。
3. 使用未被第三方重新打包损坏元数据的 GGUF。
4. 查看 `模型与Skill链路` 中最终尝试的格式和错误。

即使模型失败，Skill 输出仍应存在。如果结果为空，说明还有基础加载异常，应保留完整 ComfyUI 日志排查。

### 15.5 模型调用成功但未采用

这是候选校验在工作。常见原因：

- 返回分析、思考块、占位符或标签串。
- 删除用户锚点或加入不属于场景的元素。
- 图像提示词不是完整自然语言。
- 三视图缺少正面、90° 侧面、背面或等宽关系。
- 视频少于三段、段落互不连续或没有完整剧情。

保持默认系统提示词、关闭 `输出think块`、提高 token，并减少互相冲突的手动标签。不要为了强制采用模型而关闭一致性事实。

### 15.6 提示词重复或像锁死

依次检查：

1. 白名单是否过多。
2. 核心锁定是否过高。
3. 模板与主题池是否都固定。
4. 智能文本和额外要求是否每次完全相同。
5. 系统提示词覆盖是否过强。
6. 模型是否本身复读。

先恢复默认采样，把核心锁定降到 0-3，再提高随机强度；最后才小幅提高温度或频率惩罚。

### 15.7 风格或世界观串词

- 每次只保留一个主风格和一个主场景。
- 删除自定义补充中的旧场景、现代设备或其他媒介。
- 系列图使用 `严格风格隔离`。
- 在智能文本中明确写“不要……”并给出替代主场景。
- 检查锁定白名单是否保护了旧标签。

### 15.8 单图出现分屏、重复人物或多张脸

确认 `推荐负面词` 已实际连接到下游负向编码。清理正向中的双联画、三联画、故事板、漫画分格、多个阶段、正侧背三视图等结构词。普通单人图只描述一个当前动作；角色三视图必须通过 `设定图` 流程生成。

还要检查下游 LoRA、ControlNet 或工作流模板是否强制角色表、分镜或镜像构图。

### 15.9 视频没有模型润色

查看视频模型状态。`仅Skill` 下显示未调用是预期行为；调用成功但保留 Skill 表示候选没有通过结构或一致性校验。API 长文建议 token 至少 1800，本地小模型应关闭思考，并保持默认系统提示词。

### 15.10 终端或历史没有最新结果

运行节点一次，确认外接展示文本节点有输出，再刷新页面并重跑。终端与历史依赖前端接收执行结果；页面缓存或重复插件可能让前端脚本与后端版本不一致。

## 16. 参数设置完整手册

### 16.1 默认配置

节点当前默认组合为：`仅Skill / 生成 3 条 / 纯中文 / 标准 / 完整结果 / 平衡收敛`。

运行时随机默认关闭，内置模型上下文默认 8192，API 超时默认 120 秒。新工作流先使用默认组合确认输出，再按任务逐项调整。

### 16.2 基础参数范围

| 参数 | 默认值 | 范围/选项 | 调整建议 |
| --- | --- | --- | --- |
| `生成数量` | `3` | `1-20` | 日常 1-4，批量探索 4-8 |
| `核心标签锁定数量` | `10` | `0-500` | 大变化 0-3，稳定系列 4-10 |
| `提示词语言` | `纯中文` | 纯中文、英文提示词+中文说明、纯英文 | 按下游模型选择 |
| `详细度` | `标准` | 简洁、标准、详细 | 控制信息密度，不限制字数 |
| `输出模式` | `完整结果` | 完整结果、仅提示词优先 | 自动化下游可用仅提示词优先 |
| `图片反推最大边长` | `960` | `256-2048` | 低显存 640-768，常规 768-1024 |
| `seed` | `0` | `0-18446744073709551615` | 变化用 0，复现用正整数 |

### 16.3 内置本地模型参数

| 参数 | 默认值 | 范围 | 调整建议 |
| --- | --- | --- | --- |
| `内置上下文长度` | `8192` | `1024-327680` | 低内存 4096-8192；不要盲目拉满 |
| `内置GPU层数` | `-1` | `-1-9999` | 自动失败或显存不足时逐步降低 |
| `内置KV缓存K类型` | 平台默认 | 节点可选类型 | q8 可降低部分显存占用 |
| `内置KV缓存V类型` | 平台默认 | 节点可选类型 | 与 K 类型配合测试 |
| `内置启用思考` | 关闭 | 开/关 | 日常提示词生成关闭 |

上下文长度、图片边长和批量数量会共同增加内存占用。视觉模型还需要匹配的 mmproj，纯文本润色可选择 `无`。

### 16.4 API 参数

| 参数 | 默认值 | 范围/格式 | 调整建议 |
| --- | --- | --- | --- |
| `API超时秒` | `120` | `5-600` | 云端长文 120-180，本机服务可更低 |
| `API地址` | 服务商预设/空 | 规范 HTTP(S) Base URL | 优先使用服务商预设 |
| `API密钥` | `env:QWEN_TE_API_KEY` | 明文或 `env:变量名` | 始终推荐环境变量 |
| `API模型` | 服务商预设/空 | 有效模型 ID | 无预设服务必须填写 |
| `API额外请求头` | 空 | JSON 或每行 `Header: Value` | 只添加服务商确实要求的头 |

瞬时超时、断连、429 和服务端 5xx 会有界重试；认证错误和普通 4xx 不会重复发送。自定义地址和环境变量密钥还受可信来源检查。

### 16.5 模型采样参数

| 参数 | 默认值 | 范围 | 推荐设置 |
| --- | --- | --- | --- |
| `最大生成token` | `1800` | `128-8192` | 完整长文 1800-2600 |
| `温度` | `0.62` | `0-2` | 稳定 0.45-0.65，多样 0.72-0.9 |
| `top_p` | `0.9` | `0-1` | 0.85-0.95 |
| `top_k` | `40` | `0-200` | 稳定 20-40，多样 40-80 |
| `重复惩罚` | `1.08` | `0.5-2` | 1.05-1.15 |
| `频率惩罚` | `0` | `0-2` | 复读时 0.1-0.3 |
| `存在惩罚` | `0` | `0-2` | 扩展细节时 0.05-0.2 |
| `输出think块` | 关闭 | 开/关 | 日常关闭 |

### 16.6 预设与安全边界

预设保存生成、随机、图片反推、本地模型和采样配置，但不会保存 API 密钥、额外认证头或运行缓存。完整 ComfyUI 工作流仍可能保存控件明文，公开分享前必须清理。

### 16.7 推荐调整顺序

1. 先确认主体、主场景、主媒介和人物数量没有手动冲突。
2. 再调整模板、主题池、随机强度和锁定数量。
3. 模型复读时只先提高温度或频率惩罚中的一项。
4. 输出截断时提高 token；内存不足时降低上下文、图片边长和批量数量。
5. 最后保存为预设，避免一次修改过多参数后无法定位原因。

### 16.8 参数联动规则

- `温度 × top_p × top_k`：三者都会扩大候选范围，优先只改温度。
- `生成数量 × 最大生成token`：数量越大，总时间、费用和输出体积越高。
- `上下文长度 × 图片边长 × 显存`：两者同时提高最容易触发内存不足。
- `seed × 历史避重`：固定 seed 仍会受工作流状态和历史指纹影响。
- `锁定数量 × 白名单 × 随机强度`：白名单优先级最高，变化不足时先减少锁定。
- `模板风格 × 风格隔离 × 主题池`：模板控制媒介，主题池补题材，隔离策略处理冲突。
- `视频 Skill × 模型后置`：模型只沿 Skill 的主体、世界和因果链润色，失败时保留 Skill。

### 16.9 可直接使用的配置方案

| 用途 | 配置 |
| --- | --- |
| 稳定离线 | `仅Skill`、数量 1-3、标准、随机关闭 |
| API 长剧情 | API、数量 1-3、详细、token 1800-2600、温度 0.62-0.78 |
| 高差异批量 | 数量 4-8、随机开启、强度强、锁定 0-3、seed 0 |
| 严格系列风格 | 具体模板和主题、严格隔离、节点优先、固定 seed |
| 本地低内存 | 7B/8B Q4、上下文 4096-8192、数量 1、图片边长 640-768 |
| 角色三视图 | 设定图开启、数量 1、横向画幅、随机关闭、简洁背景 |

## 17. 分享与安全检查

发布工作流或复制给别人前确认：

- 插件目录完整，包含 `web` 和 `stage_prompt`。
- 只保留一份插件，不附带旧目录。
- 清空 ComfyUI 工作流控件中的明文 API Key 和额外认证头。
- 环境变量引用只使用接收方实际配置的变量名。
- 不分享私有 `online_search_config.json`、内部 SearXNG 地址或私人自定义标签库，除非明确需要。
- 本地 GGUF 和 mmproj 文件名在接收方机器上存在。
- 运行一次后八个输出都能返回字符串，第 2 和第 8 输出有可用正文。
- 推荐负面词已经连接到实际采样器的负向路径。
- NSFW 工作台只包含合法成年设置。

最短使用口诀：

```text
先用仅Skill验证 → 用标签和智能文本确定主线 → 再开随机或模型润色
看终端诊断 → 只修一个问题 → 保存为预设
```
